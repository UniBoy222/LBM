# LBM压力场预测神经网络加速项目 - 完整总结

**更新时间**: 2026-03-23
**状态**: ⚠️ **v3模型已完成并整理上传，100步验证达到1.47x整体加速；当前正在排查1000+步长程模拟中的密度场爆炸问题**

---

## 项目目标

使用深度学习加速Inamuro两相流LBM模拟，通过神经网络预测压力场初值，减少压力泊松方程的迭代次数，从而提升整体计算效率。

---

## 一、项目完成情况

### ✅ 1. 基准性能分析（12月完成）

运行8000步完整模拟，获得性能基准数据：

**动态阶段（步数1-1905）**：
- 压力迭代次数：10-1000次/步（平均34次）
- 单步计算时间：1.07-71.49s（平均2.71s）
- 压力求解占比：81%
- 每次迭代耗时：~79.5ms

**稳定阶段（步数1906-8000）**：
- 压力迭代次数：10次/步
- 单步计算时间：~1.08s
- 压力求解占比：61%

---

### ✅ 2. 训练数据收集（12月完成）

- **数据量**：8000对样本（input + output）
- **数据大小**：105.47 GB
- **数据格式**：二进制 `.bin` 文件
- **输入**：5通道 [ρ, u, v, w, φ]，shape (5, 48, 96, 128)
- **输出**：1通道 [p]，shape (1, 48, 96, 128)

**数据统计（全量8000步）**：
```
输入通道:
  ρ:   mean=2.5049, std=8.1988, range=[1.0, 50.0]
  u:   mean=-0.0000, std=0.0035, range=[-0.188, 0.169]
  v:   mean=0.0000, std=0.0036, range=[-0.188, 0.188]
  w:   mean=0.0000, std=0.0057, range=[-0.229, 0.229]
  φ:   mean=0.0139, std=0.0140, range=[-0.0078, 0.1089]

压力场:
  p:   mean=-0.0227, std=0.0103, range=[-0.4761, 0.7471]
```

---

### ✅ 3. 模型迭代历史

#### v1模型（直接预测绝对压力p）—— 已废弃

**模型架构**：3D U-Net（5通道输入 → p）
- 参数量：1.4M，base_features=32
- 训练集：Step 0-1524，测试集MAE: 0.00857（误差0.70%）

**问题**：
- 预测的压力峰值系统性偏大2-3倍（归一化后输出范围[-26, +137]σ）
- 作为初始猜测反而比p_prev更差，导致求解器发散NaN
- 实测：NN版本总时间557s vs Baseline 484s，**NN反而慢15%**

#### v2模型（预测Δp，8000步训练）—— 已废弃

**问题**：
- 归一化参数错误：output_std=0.001185，实际全局std=0.0000850，差14倍
- 数据不平衡：稳态数据占76%，动态阶段仅2.4%
- 训练4个epoch后停止，未完成

#### v3模型（预测Δp，Step 11-2999训练）—— 当前最新版本 ✅

**模型架构**：3D U-Net（6通道输入 → Δp）
- 输入：[ρ, u, v, w, φ, p_prev]，shape (6, 48, 96, 128)
- 输出：Δp = p_current - p_prev，shape (1, 48, 96, 128)
- 参数量：1.4M，base_features=32

**训练配置**：
- 数据范围：Step 11-2999（去掉前10步极端样本，去掉3000步后稳态数据）
- 数据划分：训练2391，验证298，测试300（80/10/10）
- 加权采样：按 sqrt(|Δp|_max) 加权，平衡动态/稳态分布
- 损失函数：Huber Loss + 0.1×梯度损失
- 优化器：Adam (lr=5e-4)，CosineAnnealingLR
- epochs：100，batch_size=4

**归一化参数（体素级，Step 11-2999）**：
```
输入: [rho, u, v, w, phi, p_prev]
  means = [2.51422158, -0.00001289, 0.0, 0.0, 0.01392343, -0.02551287]
  stds  = [8.23543629, 0.00389878, 0.00401780, 0.00503228, 0.01414641, 0.00920020]
输出: Δp
  mean = -8.3591e-06
  std  = 1.3666e-04
```

**训练结果**：
- 最佳模型：epoch 14，val_loss=0.038992
- 测试集 MAE(Δp)：0.00001394
- MAE/std 比值：0.1020（10.2%）
- 最新权重：`training/checkpoints_dp/best_model.pth`
- TorchScript模型：`training/checkpoints_dp/best_model_jit.pth`
- 2026-03-23：已整理为当前 GitHub 备份版本，便于后续跨窗口继续实验

---

### ✅ 4. 模型集成到LBM求解器（3月完成）

#### 4.1 自适应切换策略

**核心设计**：根据上一步迭代次数决定是否使用NN
```cpp
// 迭代次数 > 100 → 动态阶段 → 使用NN预测Δp
// 迭代次数 ≤ 100 → 稳态阶段 → 直接用p_prev
bool use_nn = use_neural_network && model_loaded && (last_iteration_count > NN_ITER_THRESHOLD);
```

**阈值**：`NN_ITER_THRESHOLD = 100`

#### 4.2 推理流程

```
输入物理场 [ρ, u, v, w, φ, p_prev]
    ↓
z-score归一化（6通道）
    ↓
GPU推理（~80ms）
    ↓
反归一化得到 Δp
    ↓
p_new = p_prev + Δp
    ↓
迭代求解（收敛阈值：0.001，每10步检查）
```

#### 4.3 关键参数

```cpp
// Inamuro.hpp
const std::vector<double> input_means = {2.51422158, -0.00001289, 0.0, 0.0, 0.01392343, -0.02551287};
const std::vector<double> input_stds  = {8.23543629, 0.00389878, 0.00401780, 0.00503228, 0.01414641, 0.00920020};
const double output_mean = -8.3591e-06;
const double output_std  =  1.3666e-04;
static constexpr int NN_ITER_THRESHOLD = 100;
```

---

### ✅ 5. 实际加速效果测试（3月20日完成）

#### 5.1 测试配置

- 网格尺寸：48 × 96 × 128
- 测试步数：100步
- 收敛阈值：0.001（相对误差0.1%）
- 检查频率：每10步
- Baseline数据：`out/timing_traditional.csv`（3月9日）

#### 5.2 分阶段性能对比

| 阶段 | Steps | Baseline迭代 | NN迭代 | 迭代改进 | 加速比 |
|------|-------|-------------|--------|---------|--------|
| 初始化 | 1-7 | 1000次 | 1000次 | 0% | 1.18-1.27x |
| 关键过渡 | 8-12 | 550-1000次 | 130-920次 | 8-86% | **1.31-8.06x** |
| 不稳定区 | 14-18 | 80-110次 | 100-140次 | -10~-27% | 0.74-1.06x |
| 稳态 | 19-100 | 30-90次 | 20-70次 | 0-40% | 1.15-1.94x |

#### 5.3 整体性能

| 指标 | 传统方法 | NN方法 | 改进 |
|------|---------|--------|------|
| 总计算时间（100步） | 1098s | 745s | **节省353s** |
| 整体加速比 | - | - | **1.47x** |
| GPU推理时间 | N/A | ~80ms/步 | 可忽略 |
| 收敛精度 | 0.001 | 0.001 | 相同 |

#### 5.4 关键发现

1. ✅ **过渡阶段表现最优**：Step 11-12 加速比达 7-8x，迭代减少84-86%
2. ✅ **自适应策略有效**：Step 19后自动跳过NN，避免稳态阶段引入噪声
3. ⚠️ **不稳定区（Step 14-18）**：NN预测偏差导致迭代增加，加速比<1
4. ✅ **无NaN问题**：Δp预测方案天然稳定，p_prev + Δp不会偏离太远

---

### ⚠️ 6. 当前待解决问题（3月23日）

在最新长程实验中，v3方案在约1000多步后出现密度场爆炸，当前判断重点不在模型文件损坏，而在于**压力初值接入后，压力分布函数 `hh` 与宏观压力 `p` 的一致性问题**。

近期已完成的排查与修正：
- 为 `solvePressurePoisson()` 增加了 NN 初值写入后的 `hh` 同步重建
- 将 `update_hh()` 改为与 `collision_p()` 一致的重建形式，而不是简单使用 `hh = Ei * p`
- 新增 `rho / p / |u|` 在线监控日志，便于定位失稳前兆

当前结论：
- **100步验证加速结果仍然成立**
- **长程稳定性仍需继续验证**
- 后续论文表述应区分“短程加速有效”和“长程稳定性仍在完善”两个层面

---

## 二、技术实现细节

### 2.1 核心创新点

**传统方法的问题**：
- 使用前一时刻压力作为初值
- 在动态阶段，压力场变化剧烈，初值误差大
- 需要大量迭代才能收敛

**NN加速方法的优势**：
- 预测压力增量Δp而非绝对压力，学习目标更稳定
- 加入p_prev作为第6个输入通道，提供时序信息
- 自适应切换策略，只在需要时使用NN

### 2.2 关键技术挑战及解决方案

#### 挑战1：v1模型预测绝对压力导致NaN
**解决方案**：改为预测Δp = p_current - p_prev
- Δp量级小，模型更容易学习
- 即使预测不准，p_prev + Δp也不会偏离太远

#### 挑战2：归一化参数错误（差14倍）
**解决方案**：重新计算体素级全局统计值
- 用Welford在线算法处理全量数据
- output_std从0.001185修正为1.3666e-04

#### 挑战3：数据不平衡（稳态占76%）
**解决方案**：
- 只用Step 11-2999训练（动态+过渡阶段）
- WeightedRandomSampler按sqrt(|Δp|_max)加权

#### 挑战4：前10步极端样本（|Δp|_max达18000σ）
**解决方案**：去掉Step 1-10，从Step 11开始训练

#### 挑战5：稳态阶段NN反而变差
**解决方案**：自适应切换，迭代次数≤100时跳过NN

### 2.3 v1 vs v3 对比

| 对比项 | v1（直接预测p） | v3（预测Δp） |
|--------|--------------|------------|
| 输入通道 | 5（ρ,u,v,w,φ） | 6（+p_prev） |
| 输出 | 绝对压力p | 压力增量Δp |
| 归一化 | 错误（差14倍） | 正确（体素级统计） |
| 训练数据 | Step 0-1524 | Step 11-2999 |
| 加权采样 | 无 | sqrt(|Δp|_max) |
| NaN问题 | 有 | 无 |
| 实测加速比 | -15%（反而更慢） | **+47%** |

---

## 三、论文可用的实质性成果

### 3.1 创新性

1. **Δp预测方案**：提出预测压力增量而非绝对压力，解决了绝对压力预测的稳定性问题
2. **自适应切换策略**：根据迭代次数动态决定是否使用NN，避免稳态阶段的性能退化
3. **GPU加速推理**：GPU推理~80ms，相比CPU推理295倍加速
4. **物理一致性**：基于真实模拟数据训练，保持数值精度

### 3.2 实验数据

**模型性能**：
- 测试集MAE(Δp)：0.00001394
- MAE/std比值：10.2%
- GPU推理时间：~80ms

**加速效果（100步验证）**：
- 整体加速比：**1.47x**
- 总时间节省：353s（1098s → 745s）
- 过渡阶段最大加速：**8.06x**（Step 12）
- 自适应策略：迭代次数≤100时自动跳过NN

### 3.3 论文结构建议

**标题**：基于深度学习的Inamuro两相流LBM压力场预测加速方法

**章节建议**：
1. 引言：LBM计算瓶颈分析
2. 相关工作：深度学习在CFD中的应用
3. 方法：
   - 3.1 LBM压力泊松方程
   - 3.2 Δp预测模型设计（为什么不预测绝对p）
   - 3.3 自适应切换策略
   - 3.4 GPU推理集成
4. 实验：
   - 4.1 数据生成与处理
   - 4.2 模型训练结果
   - 4.3 加速效果验证（100步对比）
   - 4.4 分阶段分析
5. 讨论：局限性与未来工作
6. 结论

---

## 四、项目文件清单

### 4.1 核心代码

```
/home/jzh/LBM/CPU/
├── Inamuro.hpp              # 核心算法（NN参数、自适应切换）
├── Inamuro.cpp              # 核心算法实现（6通道推理、Δp预测）
├── InamuroSolver.hpp        # 求解器包装
├── InamuroSolver.cpp        # 求解器实现（加载v3模型）
├── Makefile                 # 编译配置
├── params.in                # 参数文件
└── lbm_sim                  # 可执行文件
```

### 4.2 训练相关

```
training/
├── model.py                 # 3D U-Net模型定义
├── train_dp.py              # Δp训练脚本（v3）
├── dataset_dp.py            # 数据加载器（加权采样）
├── checkpoints_dp/
│   ├── best_model.pth       # 最佳模型（epoch 14）
│   └── best_model_jit.pth   # TorchScript模型
└── venv/                    # Python虚拟环境
```

### 4.3 数据

```
data/
├── train/                   # 训练数据（8000步，105.47GB）
├── sample_weights_3k.npz    # Step 11-2999的加权采样权重
└── sample_weights.npz       # 全量权重（备用）
```

### 4.4 测试结果

```
out/
├── timing_traditional.csv   # Baseline性能数据（8000步）
└── ...
nn_v3_100step.log            # v3 NN 100步测试日志
baseline_10step.log          # Baseline 10步测试日志
```

---

## 五、关键经验总结

### 5.1 成功经验

1. **预测增量而非绝对值**：Δp比p更容易学习，且天然稳定
2. **正确的归一化**：体素级统计比样本级统计更准确
3. **数据范围选择**：只用动态阶段数据，避免稳态数据稀释信号
4. **自适应切换**：不是所有步骤都需要NN，按需使用才能最大化收益
5. **GPU推理**：~80ms推理时间可忽略不计

### 5.2 失败教训

1. **v1直接预测p**：峰值偏大2-3倍，导致NaN，整体反而变慢
2. **归一化参数错误**：output_std差14倍，导致训练信号被压扁
3. **全量数据训练**：稳态数据占76%，严重稀释动态阶段的学习信号

### 5.3 局限性

1. **不稳定区（Step 14-18）**：NN预测偏差，加速比<1
2. **模型泛化能力**：仅在当前参数配置下验证
3. **GPU依赖**：CPU推理太慢（28s），需要CUDA支持

### 5.4 未来改进方向

1. **改善不稳定区**：针对Step 14-18的数据分布做专项优化
2. **更细粒度的自适应策略**：不仅基于迭代次数，还可基于压力场变化率
3. **泛化能力验证**：在不同参数（液滴大小、密度比等）下测试
4. **模型压缩**：减小模型大小，进一步降低推理时间

---

## 六、使用说明

### 6.1 编译

```bash
cd /home/jzh/LBM/CPU
make clean && make
```

### 6.2 运行（启用NN加速，自适应切换）

```bash
./lbm_sim
```

### 6.3 运行（禁用NN，对比测试）

修改 `InamuroSolver.cpp`：
```cpp
inamuro->setUseNeuralNetwork(false);
```
重新编译并运行。

### 6.4 调整自适应阈值

修改 `Inamuro.hpp`：
```cpp
static constexpr int NN_ITER_THRESHOLD = 100; // 调整此值
```

---

**项目状态**: ✅ 完成（100步验证，1.47x加速）
**文档版本**: v4.0
**最后更新**: 2026-03-20
