# 项目协作规则

- 全程使用中文，回复简洁。
- 不重复用户已提供的背景和结果。
- 简单任务直接执行，不写长计划。
- 搜索、读取和测试尽量批量并行执行。
- 只读取必要的文件片段，禁止无目的读取完整大日志、CSV 或源码。
- 命令输出只汇报结论和关键错误，不粘贴长日志。
- 长任务启动后使用等待机制，禁止反复执行 `tail`、`find`、`test` 轮询。
- 仅在任务完成、发生错误或需要用户决策时汇报。
- 中间进度只报告有实质变化的里程碑。
- 不重复已经通过的编译和测试，除非代码发生相关变化。
- 实验先跑最小 smoke，再跑短程验证，最后才跑长程。
- 不主动开展请求范围外的重构、实验或文档扩写。
- 最终回复默认不超过 300 个汉字，除非用户明确要求详细说明。
- 最终只包含：完成内容、关键结果、验证状态、必要的下一步。

## 神经网络训练执行规则

- 所有神经网络 smoke、单样本过拟合、超参数实验和正式训练统一在 RTX 5080 WSL2 上执行，禁止在当前 Linux 机器训练。
- 远程连接：`ssh jzh@rtx5080-wsl`（Tailscale + SSH）。
- 远程项目路径：`/home/jzh/PINN/LBM`。
- 远程 Python 环境：`/home/jzh/PINN/LBM/PINN_Poisson/.venv`。
- 远程训练必须在 `/home/jzh/PINN/LBM` 下运行，禁止直接在 `/mnt/d` 下训练。
- 当前 Linux 机器只负责代码修改、真实 `hh[15]` 数据生成、GPU 求解器测试及 200/1000/8000-step 闭环验证。
- 训练前使用 `rsync` 同步代码、manifest 和必要数据到 RTX 5080。
- 每次训练使用独立 run 名称，禁止覆盖已有有效模型。
- 正式训练前必须依次通过：CUDA 识别检查、1-epoch smoke、单样本过拟合。
- 远程训练使用 `tmux` 后台运行，日志写入 `PINN_Poisson/logs`；禁止频繁轮询，只在预计完成时间或关键里程碑检查。
- 训练完成后，将模型、训练指标和日志同步回当前 Linux 机器，并备份到 RTX 5080 的 `/mnt/d/PINN_Training`。
- 除非用户明确批准，不得因 SSH 或同步问题退回当前机器训练。
- 首次接入只验证 SSH、RTX 5080、远程项目和虚拟环境，不立即启动长训练。
